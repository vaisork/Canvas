name: Fix test index images
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Corrección automática pausada; las fotos del index se gestionan directamente."
