const canvasSpaces={canvas90:'Canvas 90',jardin:'El Jardín',deck:'Canvas Deck',origen:'Canvas Origen',pulso:'Canvas Pulso'};function demoSlots(){return['08:00','09:00','10:00','11:00','12:00','13:00','14:00','15:00','16:00','17:00','18:00','19:00'].map((t,i)=>({time:t,status:[2,3,7,8].includes(i)?'busy':'free'}))}function renderSlots(targetId){const el=document.getElementById(targetId);if(!el)return;el.innerHTML=demoSlots().map(s=>`<div class="slot ${s.status}"><strong>${s.time}</strong><br><span>${s.status==='free'?'Libre':'Ocupado'}</span></div>`).join('')}document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-demo-slots]').forEach(el=>renderSlots(el.id));document.querySelectorAll('.space-selector button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.space-selector button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const title=document.querySelector('[data-space-title]');if(title)title.textContent=canvasSpaces[btn.dataset.space]||btn.textContent;}));});